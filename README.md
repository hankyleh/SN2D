# SN2D
2-dimensional discrete ordinates transport code for NE 523 computer projects

## Compilation Instructions

The makefile handles all of the compilation. To compile, just run the command `make` in either the main directory, or `/src`.

## Execution Instructions

Input files should use the .txt extension, and the program is run using `bin/sn2d filename.txt`.

## Code Status

